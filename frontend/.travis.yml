sudo: required
services:
  - docker

before_install:
    - docker build -t dijanabrlikj/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true dijanabrlikj/docker-react npm run test -- --coverage  